{
  "name": "muban-page-transition-controller",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "start": "yarn && yarn watch",
    "watch": "tsc -w -d --skipLibCheck",
    "build": "rimraf dist && yarn build:ts && yarn build:ts-esm",
    "build:ts": "tsc -d --skipLibCheck",
    "build:ts-esm": "tsc -p tsconfig.esm.json -d --skipLibCheck",
    "prepublishOnly": "yarn build"
  },
  "author": "Mick Jasker <mick.jasker@mediamonks.com>",
  "license": "MIT",
  "devDependencies": {
    "@mediamonks/eslint-config-base": "^1.0.2",
    "eslint": "^7.32.0",
    "ts-node": "^9.0.0",
    "typescript": "^4.4.2"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "prettier": "^2.3.2",
    "seng-disposable-event-listener": "^2.0.1",
    "seng-disposable-manager": "^2.0.0"
  }
}
